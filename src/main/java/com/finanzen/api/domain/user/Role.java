package com.finanzen.api.domain.user;

/**
 * Enumeration defining the access levels (roles) within the application.
 * <p>
 * This pure domain enum dictates the authorization boundaries for users.
 * </p>
 */
public enum Role {
    ADMIN,
    USER
}
